# vtable-guild 现状分析与影响力增长计划

> 采集日期：2026-07-30。本文所有数字均为当日实测（npm registry API / GitHub API / 本地构建产物），非估算。
> 本次分析为**只读**，未修改任何源码。

---

## 一、结论先行

**这个项目的问题不是"做得不够好"，而是"没有人能看见它、也没有人能试用它"。**

代码质量、架构设计、测试与性能纪律，都明显高于一个 11 star 项目的平均水位——17k 行源码、87 个测试用例、四层性能度量体系、三套 CSS 消费模式、24 页文档站、完整 CI/CD 与 changesets 发布流水线。功能覆盖已接近 ant-design-vue Table 的完整面。

但增长漏斗在**第一跳**就断了：

| 漏斗环节 | 现状                                                                 | 判定    |
| -------- | -------------------------------------------------------------------- | ------- |
| 被发现   | GitHub topics = **空**；无技术文章、无社区曝光                       | ❌ 断裂 |
| 被理解   | README **不含任何指向文档站的链接**；无截图/GIF                      | ❌ 断裂 |
| 被试用   | 文档站 **零个可交互 demo**（`PlaygroundDemo` 组件已注册但 0 页引用） | ❌ 断裂 |
| 被信任   | 文档存在会让人直接踩坑的错误（`size` 尺寸命名）                      | ⚠️ 受损 |
| 被采用   | 484 下载/月、47 下载/周                                              | 结果    |

**表格组件是"所见即所得"型产品——demo 就是产品本身。一个看不到、点不动的表格，不可能被采用。**

后文的行动计划按"修漏斗"而非"加功能"排序。**P0 阶段（约 2 周、不写一行组件代码）预计能带来最大边际收益。**

---

## 二、项目现状：硬数据

### 2.1 外部指标（2026-07-30 实测）

| 指标                        | 数值                                                              |
| --------------------------- | ----------------------------------------------------------------- |
| GitHub star                 | **11**                                                            |
| fork / watcher / open issue | 0 / 1 / 1                                                         |
| 仓库创建                    | 2026-02-10（约 5.7 个月）                                         |
| 最后 push                   | 2026-07-14（**已静默 16 天**）                                    |
| GitHub topics               | **[] 空**                                                         |
| 文档站                      | https://parade0393.github.io/vtable-guild/ （HTTP 200，正常在线） |
| npm 最新版本                | 2.3.0（首发 2026-04-10，共 18 个版本）                            |
| npm 下载                    | **484 / 月**，**47 / 周**                                         |
| npm 包体积                  | 867 KB unpacked，449 files                                        |
| License                     | MIT                                                               |

> 484/月 的量级基本等于"镜像站抓取 + 少量好奇开发者"，尚未出现稳定的真实项目依赖。

### 2.2 代码规模与工程化

| 包                           | 文件数 | 行数       | 发布状态                   |
| ---------------------------- | ------ | ---------- | -------------------------- |
| `@vtable-guild/table`        | 62     | 10,155     | private（内部）            |
| `@vtable-guild/core`         | 40     | 4,691      | private（内部）            |
| `@vtable-guild/theme`        | 29     | 1,774      | private（内部）            |
| `@vtable-guild/icons`        | 18     | 344        | private（内部）            |
| `@vtable-guild/vtable-guild` | 4      | 118        | **public（唯一发布入口）** |
| 合计                         | 153    | **17,082** |                            |

- 测试：**87 个用例**，21 个测试/基准文件，覆盖 9 个 composable + 组件渲染 + 主题合并 + 类型契约
- 基准：`useSorter` / `useFilter` / `useSelection` / `useTreeData` 四条 L1 微基准，含确定性 fixture（刻意不用 `Math.random()`，避免命中排序最优路径）
- CI：`ci.yml`（lint + type-check + test + build）、`release.yml`（changesets + npm trusted publishing）、`deploy-site.yml`（GitHub Pages）
- 提交：222 commits / 5.7 个月，规范 conventional commits，走 PR + changeset 流程

### 2.3 产物体积（本地构建实测，gzip）

| 产物                             | raw    | gzip       |
| -------------------------------- | ------ | ---------- |
| JS 全量（所有模块）              | 250 KB | **56 KB**  |
| 最大单块 `Table.mjs`             | 24 KB  | 6.0 KB     |
| CSS `style.css`（prebuilt 全量） | 58 KB  | **9.6 KB** |
| CSS `tailwind4.css`              | 16 KB  | 3.9 KB     |

构建采用 `preserveModules`，JS 可 tree-shake，实际引入量低于 56 KB。运行时仅一个依赖 `tailwind-variants`，peer 仅 `vue ^3.5.0`——**依赖面干净，这是一个可以拿出来讲的优势**。

---

## 三、实现分析

### 3.1 架构

```
@vtable-guild/vtable-guild   （聚合入口，唯一 public 包）
  ├── table   VTable + 9 composables + 24 子组件（全 TSX）
  ├── theme   纯数据：tv slots/variants + CSS 变量 token（antdv / element-plus 双预设）
  ├── core    tv()/cn() 封装、useTheme 三层合并、Vue 插件、基础 UI、VirtualList（vendored）
  └── icons   SVG 组件
```

**三层主题模型**（默认主题 → 全局 config → 实例 `ui`/`class`）是本项目的核心设计资产。`useTheme` 的实现值得单独指出：

- `_slotFns` 用 `computed` 缓存 merge + `tv()` 结果，**仅 variant props 变化才重算**，`ui`/`class` 变化不触发
- 返回的 slot 函数**引用稳定**（setup 阶段创建一次），内部闭包懒读 computed 保证响应性——避免了每次渲染重建函数导致的子组件无效更新
- 分字段合并策略明确且有注释：`slots` 走 tailwind-merge 智能合并、`variants` 深合并、`defaultVariants` 浅合并、`compoundVariants` 追加

这是真正理解了 tailwind-variants + Vue 响应式交互成本后写出来的代码，不是照抄。

### 3.2 功能覆盖

已实现（对齐 ant-design-vue Table 心智）：排序（受控/非受控双轨）、筛选（多选/单选/树形/自定义 dropdown）、行选择（checkbox/radio/selections 下拉/checkStrictly）、展开行、树形数据、固定列与固定表头、多级表头、单元格合并（`rowSpan`/`colSpan`/`customCell`）、列宽拖拽、title/footer/summary、sticky、虚拟滚动、内置 locale、`EXPAND_COLUMN`/`SELECTION_COLUMN` 占位常量、三套 CSS 消费模式（prebuilt / tailwind3 / tailwind4）、class 前缀隔离。

刻意不做：分页（用户自带）。

**未覆盖**（对比 vxe-table 这类"企业全能表格"）：单元格编辑、Excel 导出、键盘导航与单元格焦点、列拖拽换序、右键菜单。

### 3.3 已识别的技术债与风险

| 项                          | 说明                                                                                                                                                                                                       | 影响                                                        |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| **文档与代码不一致**        | 3 处文档（`comparison/index.md`、`comparison/enhancements.md`、`guide/theme-overrides.md`）称尺寸为 `sm/md/lg`，实际 prop 只接受 `'small' \| 'middle' \| 'large'`。`useTheme` 的 JSDoc 示例也写 `sm/md/lg` | **高**：用户照文档写 `size="md"` 静默失效，直接损伤第一印象 |
| **键盘可达性缺失**          | `packages/table/src` 内 `tabindex`/`keydown` 命中数为 **0**；排序头、筛选图标不可键盘操作（`aria-sort`、部分 `aria-label`/`role` 已有，共 30 处）                                                          | 中：阻挡有无障碍合规要求的团队（政企/外企）                 |
| **子包全部 private**        | 只能整包引入，无法只装 `core`+`theme` 做二次开发                                                                                                                                                           | 中：限制"作为主题引擎被复用"的路径                          |
| **包体积/文件数**           | 867 KB / 449 files unpacked                                                                                                                                                                                | 低-中：`preserveModules` 的代价，可接受但会被挑剔           |
| **单人维护 + 已静默 16 天** | 0 fork、1 watcher，无 CONTRIBUTING/issue 模板                                                                                                                                                              | 中：企业选型时的"巴士因子"风险点                            |
| **roadmap 严重滞后**        | `docs/roadmap.md` 全是未勾选 checkbox，实际已完成到阶段八                                                                                                                                                  | 低：但访客据此会误判项目停滞                                |

---

## 四、定位分析

### 4.1 坐标系（同日实测 star）

| 项目             | star      | 性质                                            |
| ---------------- | --------- | ----------------------------------------------- |
| TanStack/table   | 28,244    | Headless 表格逻辑层，无样式                     |
| element-plus     | 27,640    | 完整 UI 库                                      |
| ant-design-vue   | 21,603    | 完整 UI 库                                      |
| shadcn-vue       | 10,360    | 复制粘贴式组件集                                |
| **vxe-table**    | **8,605** | Vue 企业级全能表格（最直接的"表格赛道"标杆）    |
| nuxt/ui          | 6,795     | Tailwind 驱动 UI 库（本项目主题模型的灵感来源） |
| **vtable-guild** | **11**    | 单组件 + 主题引擎                               |

### 4.2 真实赛道

vtable-guild **不在**"完整 UI 库"赛道（不与 antdv/element-plus 竞争），**也不在**"headless 逻辑层"赛道（它自带样式）。它的位置是一个更窄的夹层：

> **给已经在用 antdv / element-plus 的团队，做一个视觉无缝、API 心智不变、但补齐虚拟滚动与样式覆盖能力的表格替换件。**

用一句话概括差异化：

- 对 **TanStack Table**：你不用自己写样式，开箱就长得像你现有的 UI 库
- 对 **vxe-table**：你不用引入第二套视觉体系，也不用学一套新 API
- 对 **antdv/element-plus 原生 Table**：你拿到虚拟滚动 + 三层样式覆盖，且不用换 UI 库

### 4.3 护城河与软肋

**真实护城河（别人不容易抄）**

1. 三层主题合并 + tailwind-variants slot 模型的**完整工程化落地**（含三套 CSS 消费模式、class 前缀隔离、CSS layer 共存方案）
2. **多预设皮肤**：同一套表格逻辑切换 antdv / element-plus 外观，仅改 JS 一行
3. 依赖面极干净（1 runtime dep），体积可控
4. 已具备的性能纪律（四层度量 + 基准 fixture），是可持续优化的地基

**结构性软肋（必须承认）**

1. **定位是"比较级"的**——价值主张必须先让用户承认"我现有表格有问题"，认知成本天然高于"这是一个新表格库"
2. **单组件库的分发劣势**：没有 UI 库的"顺带被安装"效应，只能靠精准搜索命中
3. **可信度赤字**：11 star / 0 fork / 单人维护，是企业选型的硬门槛
4. **README 中的宣称需要经得起复核**（`element-plus` 已有 `el-table-v2`；`vxe-table` 覆盖面更广）——任何一处夸大都会被资深评估者抓住并放弃

---

## 五、影响力诊断：为什么是 11 star

按漏斗逐层归因，**按影响力排序**：

### D1（致命）文档站零可交互 demo

`site/.vitepress/theme/components/PlaygroundDemo.vue` 已实现并注册为全局组件，但**在 24 个 md 页面中被引用 0 次**（实测 `grep -rl PlaygroundDemo site/ --include="*.md"` 返回空）。且该组件的实现是 `iframe src="http://localhost:5173"`——**即使被引用，在线上也只会是空白框**。

结果：线上文档站是纯静态 markdown + 代码块。**访客无法看到一张真实的表格，更无法点击排序、拖动滚动条、感受虚拟滚动。** 对表格库而言这等于产品不可试用。

### D2（致命）README 不通向文档站

README 只有 2 个 badge（DeepWiki、npm version），**全文无一处链接 `parade0393.github.io/vtable-guild`**（GitHub 仓库 homepage 字段已设置，但那个位置极易被忽略）。24 页精心撰写的文档，绝大多数访客根本不知道存在。README 也没有任何截图/GIF——`cover/` 目录**是空的**。

### D3（严重）零被动发现渠道

- GitHub `topics = []`：放弃了 `vue3`/`table`/`virtual-scroll`/`ant-design-vue`/`element-plus`/`tailwindcss` 等标签带来的免费搜索流量
- 无任何技术文章、社区帖、视频
- 222 个 commit 全部指向内部建设，**分发投入 ≈ 0**

### D4（中）语言错配

README 是英文（含中文版），文档站是纯中文。而目标用户是 antdv/element-plus 使用者——以中文生态为主。当前配置等于用英文首页拦截中文用户，再用中文文档拦截英文用户。

### D5（中）可信度信号缺失

无 CONTRIBUTING、无 issue/PR 模板、无覆盖率 badge、无 CHANGELOG 入口链接、`roadmap.md` 通篇未勾选（看起来像刚起步的坑）。

### D6（中）文档正确性

`size` 尺寸命名三处错误（见 3.3）。评估者踩到一次静默失效，通常不会提 issue，而是直接关掉页面。

---

## 六、行动计划

原则：**先修漏斗，再加功能。** 每项含"为什么 / 做什么 / 验收标准"。

### P0 — 两周内，不写组件代码，只修分发（最高优先级）

#### P0-1 让文档站可交互（对应 D1）

- **为什么**：这是唯一能把"访客"变成"试用者"的动作，其他所有投入都依赖它
- **做什么**：
  - 把 VitePress 配成能直接在 md 中渲染真实 `<VTable>`（本地 workspace 包 alias + 引入 CSS），淘汰 localhost iframe 方案
  - 为 11 个功能页（排序/筛选/选择/展开/树形/固定列/虚拟滚动/列宽拖拽/多级表头/摘要行/插槽）各配 **1 个可交互 demo + 可折叠源码**
  - 虚拟滚动页配 **10 万行** demo——这是最有说服力的单个页面
  - 首页 hero 下方直接嵌一张"能点"的表格
- **验收**：线上任一功能页可点击排序、拖拽列宽、滚动虚拟列表；无任何 localhost 引用

#### P0-2 重建 README 首屏（对应 D2）

- **为什么**：GitHub README 是流量总入口，当前它是断头路
- **做什么**：
  - 首屏加 **GIF**（推荐：10 万行虚拟滚动流畅滚动 + 一行代码切换 antdv/element-plus 皮肤，后者是最独特的卖点）
  - 首屏加显著的 **📖 文档 / 🎮 在线体验** 链接
  - 补 badge：CI 状态、license、bundle size（gzip 56 KB JS + 9.6 KB CSS——这是硬优势）、npm 下载量
  - 把"为什么不用原生表格"从段落改成 3 行对比表，30 秒内可读完
- **验收**：新访客在 README 停留 15 秒内即可看到动图、知道文档地址

#### P0-3 打开被动发现（对应 D3）

- **做什么**：设置 GitHub topics（建议：`vue`、`vue3`、`table`、`datatable`、`virtual-scroll`、`tailwindcss`、`ant-design-vue`、`element-plus`、`typescript`、`component-library`）；npm keywords 补 `virtual-scroll`、`datatable`、`data-grid`、`vue-table`、`tailwind-variants`
- **验收**：topics ≥ 8 个；npm 页面关键词覆盖主要搜索词

#### P0-4 修正可信度硬伤（对应 D5、D6）

- **做什么**：
  - 修正 3 处 `size` 文档错误 + `useTheme` JSDoc 示例（或反向：给 prop 增加 `sm/md/lg` 别名，但**必须二者一致**）
  - `roadmap.md` 补齐已完成项勾选，或改写为"已完成 / 进行中 / 规划中"三段式
  - 加 CONTRIBUTING.md、issue/PR 模板、SECURITY.md
  - README 链接 CHANGELOG
- **验收**：文档内所有可复制代码片段实测可运行

### P1 — 一个月内，主动分发

#### P1-1 一个可复现的性能对照页（弹药）

- **为什么**：所有后续传播都需要一个"不可反驳的证据"，而项目已有 `/perf` 演练台和四层度量体系作为基础
- **做什么**：把 playground `/perf` 升级为公开可访问页面，对 vtable-guild / antdv Table / el-table-v2 在同一批数据（1k / 1w / 10w 行）下对照，公开采集方法与环境，给出 scripting 耗时、INP、内存
- **验收**：任何人可自行打开复现；README 引用其结论

#### P1-2 三篇技术文章（讲实现，不讲广告）

选题按"技术深度 > 产品宣传"排序，这类内容在中文技术社区的传播效率远高于"我做了个组件库"：

1. **《用 tailwind-variants 做三层主题合并：一个表格组件的样式覆盖体系》**——本项目最独特的资产，且是通用知识，受众远大于表格用户
2. **《Tailwind v4 的 @layer 与 antd reset 冲突：一个必须用 CSS Cascade Layers 解的坑》**——README 里已有的完整排查结论，是高搜索价值的独立知识点
3. **《给表格组件建性能标尺：从微基准到浏览器剖析的四层体系》**——`docs/performance.md` 已有内容，改写即可

- **渠道**：掘金 / 思否 / 知乎专栏 / linux.do（致谢里已有社区归属）/ Vue 中文社区周刊投稿
- **验收**：3 篇发布，每篇文末含仓库与文档链接

#### P1-3 精准场景投放

在 ant-design-vue / element-plus 相关 issue 与讨论中（例："antdv Table 无虚拟滚动"、"el-table 大数据卡顿"、"表头对齐多出一列"），**以解决问题的姿态**回复，附最小可运行 demo。注意：只在确实对症的帖子回复，不刷。

- **验收**：10 条高质量回复，来源可追踪（文档站加 UTM 或 referrer 观察）

#### P1-4 语言分层（对应 D4）

- **做什么**：文档站开双语，**默认中文**（匹配真实用户群），英文版覆盖 getting-started + api-reference 即可；README 中文版置顶
- **验收**：`/en/` 路径可用，中文为默认落地

### P2 — 三个月内，把"能用"变成"敢押上生产"

#### P2-1 键盘可达性与 a11y 补齐

- **为什么**：这是当前唯一的"合规级"缺口，且 vxe-table 等竞品同样薄弱——**这是一个可占据的差异点**，而非追赶项
- **做什么**：可聚焦的排序头/筛选触发器（`tabindex` + Enter/Space）、筛选面板焦点陷阱与 Esc 关闭、`aria-rowcount`/`aria-colindex`、可见焦点环；文档加 a11y 说明页
- **验收**：全键盘可完成排序+筛选+选择；Lighthouse a11y ≥ 95

#### P2-2 开放子包（对应 3.3）

把 `core` + `theme` 转为 public，支持"只要主题引擎不要表格"的用法。这会打开一条全新的被引用路径（其他组件作者复用三层主题模型）。

#### P2-3 一个真实落地案例

最有效的信任建立方式。可以是自己的项目，也可以帮一个中小开源后台模板（vue-pure-admin / vue-vben-admin 生态类）接入并写迁移记录。

- **验收**：至少 1 个公开可访问、非作者本人玩具项目的使用记录

#### P2-4 按需扩展功能（严格按需，勿主动堆）

优先级排序（仅当有真实用户提出时才做）：单元格编辑 > 列拖拽换序 > Excel 导出。
**明确不建议现在做**：这些是 vxe-table 的主场，在漏斗未修好之前投入功能开发是最典型的资源错配。

---

## 七、指标体系

抛弃 star 作为主指标（虚荣指标，且易被单次曝光扭曲）。建议按漏斗设指标：

| 层级           | 指标                  | 当前   | 3 个月目标           |
| -------------- | --------------------- | ------ | -------------------- |
| 曝光           | 文档站独立访客/月     | 未埋点 | **先埋点**，再定目标 |
| 试用           | npm 下载/周           | 47     | 300+                 |
| 留存           | 连续 4 周下载不下滑   | —      | 达成                 |
| 参与           | issue + discussion 数 | 1      | 15+                  |
| 信任           | fork 数               | 0      | 5+                   |
| 采纳           | 可验证的真实项目使用  | 0      | 2+                   |
| 虚荣（仅观察） | star                  | 11     | 150+                 |

**第一件该做的事其实是埋点**（文档站接一个隐私友好的分析，如 Umami/Plausible 自托管或 Cloudflare Web Analytics）——否则 P1 的所有分发动作都无法归因。

---

## 八、明确不建议做的事

1. **不要继续加功能来"追上 vxe-table"**——功能覆盖已不是瓶颈，且正面拼全能表格必败
2. **不要刷 star**（互 star 群、买量）——会污染唯一还能反映真实兴趣的信号，且对下载量无帮助
3. **不要重写为"完整 UI 库"**——放弃了"零迁移成本替换件"这个唯一清晰的定位
4. **不要在漏斗修好前做大规模宣发**——把流量导向一个无 demo 的文档站，是一次性消耗掉最宝贵的首次印象
5. **不要在 README 里做未经复核的对比宣称**——资深评估者一旦抓到一处，会连带否定全部；`element-plus` 有 `el-table-v2`、`vxe-table` 功能更广，这些都要如实承认（在承认的前提下讲清自己的差异，反而更可信）

---

## 九、风险与前提

| 风险               | 说明                                                                                        | 缓解                                                                                     |
| ------------------ | ------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| **单人精力**       | 5.7 个月 222 commit 已是高强度；P0+P1 是额外的"非编码"工作，且是作者可能不擅长/不喜欢的类型 | P0 四项可拆成 8 个半天完成；先做 P0-1（demo）与 P0-2（README），这两项贡献了绝大部分收益 |
| **已静默 16 天**   | 访客看到"最后更新 2 周前"仍在容忍范围，但若延至 2 个月会显著劝退                            | 保持至少月度可见更新（文档更新也计入）                                                   |
| **定位的认知成本** | "比较级"价值主张无法靠一句话说服，必须靠 demo 与性能对照                                    | 这正是 P0-1、P1-1 的目的                                                                 |
| **中文生态天花板** | 目标用户集中在中文圈，规模有上限                                                            | 先在中文圈做实（默认中文），英文作为增量而非主战场                                       |

---

## 十、执行顺序（一页速查）

```
第 1 周   ① 文档站埋点            ② GitHub topics + npm keywords
          ③ 修 size 文档错误       ④ roadmap 状态修正
第 2 周   ⑤ 文档站可交互 demo（11 页 + 首页 + 10 万行虚拟滚动）  ← 最关键
          ⑥ README 重做（GIF + 文档链接 + badge + 对比表）
第 3-4 周 ⑦ 公开性能对照页        ⑧ CONTRIBUTING / issue 模板
第 2 月   ⑨ 三篇技术文章          ⑩ 精准场景投放  ⑪ 文档双语（默认中文）
第 3 月   ⑫ a11y 键盘可达         ⑬ core/theme 转 public  ⑭ 真实落地案例
```

**如果只能做一件事：把文档站的 demo 做出来（P0-1）。** 其余所有努力的转化率都取决于它。
