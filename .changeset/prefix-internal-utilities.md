---
'@vtable-guild/vtable-guild': minor
---

Prefix internal prebuilt utility classes with `vtg-` by default, add configurable `classPrefix`, and add a Tailwind CSS 4 mode that keeps internal utilities unprefixed.
