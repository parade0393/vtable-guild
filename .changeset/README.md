# Changesets

This directory is managed by Changesets.

- Run `pnpm changeset` to create a release note for package changes.
- Run `pnpm run version:packages` to apply version bumps locally.
- The release workflow will publish public packages when changesets are present on `master`.
