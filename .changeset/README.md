# Changesets

This directory is managed by Changesets.

- Run `pnpm changeset` to create a release note for package changes.
- Run `pnpm version` to apply version bumps locally.
- The release workflow will publish public packages when changesets are present on `main`.
