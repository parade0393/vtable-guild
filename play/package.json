{
  "name": "@vtable-guild/play",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "type-check": "vue-tsc --project ./tsconfig.json --noEmit",
    "test": "vitest run --config ../vitest.config.ts --project play --passWithNoTests"
  },
  "dependencies": {
    "@vue/repl": "^4.6.1",
    "vue": "^3.5.0"
  }
}
